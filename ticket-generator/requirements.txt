# Dependencias del Generador de Tickets
# Obligatorias (modo asistido y lectura/escritura de Excel):
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
openpyxl==3.1.5

# Opcional (solo para el MODO AUTOMÁTICO que controla el navegador):
#   playwright==1.45.0
# Después de instalarla ejecutar una vez:  python -m playwright install chromium
playwright==1.45.0
